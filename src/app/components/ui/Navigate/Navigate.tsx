import Link from "next/link";
import { usePathname } from "next/navigation";

const Navigate = () => {
  return (
    <nav className="flex flex-col md:flex-row flex-1 justify-center items-center px-5 bg-red-500">
      {/** Navigate desktop  */}
      
      {/** Navigate mobile  */}
    </nav>
  );
};
export default Navigate;
